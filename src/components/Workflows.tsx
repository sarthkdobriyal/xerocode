import { FC } from 'react'

interface WorkflowsProps {
  
}

const Workflows: FC<WorkflowsProps> = ({}) => {
  return <div className='h-screen w-full border'>
    <div className='text-center w-full border bg-gradient-to-r from-[#4E4BE4] to-[#E862FE] text-3xl tracking-widest py-2 bg-clip-text text-transparent'>Use AI to handle workflows in your chat platforms.</div>
  </div>
}

export default Workflows

